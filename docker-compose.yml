services:
  frontend:
    image: chswell/chswell:latest
    container_name: frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    security_opt:
      - apparmor:unconfined

    restart: unless-stopped